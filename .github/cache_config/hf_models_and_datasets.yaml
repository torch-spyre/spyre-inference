
# List of HF models to cache.
# public_models: downloaded directly by the test workflow (no token needed).
# models: gated models, cached via the add_or_remove_artifacts_from_cache workflow.
public_models:
  - ibm-granite/granite-4.1-3b

models:
  - meta-llama/Meta-Llama-3-8B
  - meta-llama/Llama-3.2-1B-Instruct
